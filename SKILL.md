---
name: live-poster-codex
description: 直播物料准备与海报生成工具。用于银行螺丝钉直播物料准备、飞书物料文档插入、二维码检测下载、生成6张海报（学院、翻写、回放、预告+企微、新预告+企微、横版预告）、飞书共享空间上传、群通知和团队群定时通知。
---

# 直播海报制作

根据直播链接、飞书大纲文档和群内二维码信息生成6张直播海报，并完成物料文档、飞书上传、群通知和团队定时通知流程。

> ⚠️ **本文档下方部分历史内容（关键词4位示例、旧二维码映射）已过时，以下「最新定版」为准。**

## 🔴 最新定版（2026-06，458期起，优先级最高）

**1. 关键词改为6位**：格式 `2位年+2位月+2位日`（如 2026年6月30日→`260630`），旧的4位（月+日）已废弃。三处必须一致：海报上显示、小鹅通后台回复关键词、发胡亮消息里的「关键词」。

**2. 模板已换新版**（template_new_1~6.png，括号撑宽到 469px，6位关键词满格116号不压缩）。`template_config.py` 的 date_code_box / qr_box 已按新模板像素级重新校准，**勿擅改**：
- 关键词框 date_code_box（按角括号「」完整跨度水平+垂直对中）：
  t2翻写/t3回放=[750,4169,458,106]、t4预告=[876,4161,458,106]、t5新预告=[1088,2871,458,106]、t6横版=[2546,1539,458,106]
- 二维码框 qr_box：竖版t2/3/4=[916,3381,583,557]、t5=[290,2654,408,408]（已下移5px）、t6横版=[2468,403,880,880]（见下方第8条，缩到框内）

**3. 关键词渲染**（generate_image.py）：整串一次性 draw.text（与时间数字同方式），思源黑体粗体116号，**无描边、不逐字、不压缩**；仅超宽时自动缩字号兜底。（用户确认保持无描边；若以后要加粗匹配模板中文，加 stroke_width=3。）

**4. 换模板后必校准**：用脚本检测橙色括号簇得 date_code_box、检测浅色边框包围盒得 qr_box，**两项都要校准**（横版曾因只改关键词框导致二维码错位）。

**5. 标题（默认两行大字号，勿随意压行）**：生成海报时**默认不传 title_max_lines**（即默认=2行，自动取大字号：竖版起始148、横版起始180，只有超过2行才缩小）。`title_max_lines=1` 会强制压成一行、字号大幅缩小（横版会从180缩到108，明显变小），**只在用户当期明确要求"标题压一行"时才传，且不得延续到其它期**。2026-07-06（458期）教训：把456期的一次性单行需求延续到458，导致标题被压小，用户不满。

**6. 标题/文案字号：设计字号不可擅改（2026-07-09 更新，最高优先级）**
设计字号：**竖版标题148、横版标题180、介绍文案最小96（横竖版都不低于96）**。

- **规则①（勿擅改最小字号）**：任何情况下，标题和介绍文案的**最小字号规则都不允许擅自改动**。确有必要改动时，**必须先经人工（用户）同意**，不得自行缩小。—— 曾发生：把456的一次性"标题压一行(title_max_lines=1)"延续到458，导致标题被压小，用户强烈不满。
- **规则②（单字落单先改行宽）**：遇到"单行只剩一个字"（孤字），**优先加宽该行的行宽**（标题改 `title_max_width`、文案改 `caption_max_width`，让该行能多容纳、把孤字带上来或整句排一行），**绝不靠缩小字号来解决**。
- 具体：标题框左侧固定，只允许向右侧加宽，避免标题第二行只剩一个字或标点，**绝不靠缩小字号解决孤字**。模板1-5按当前 `template_config.py` 定版执行；横版模板6定版 `title_x=190`、`title_max_width=2200`、`title_font_size=180`，右侧仍需避开二维码框。`caption_max_width` 定版竖版t2/t3/t4=1950、横版t6=1950、模板5=1930；介绍文案默认 `caption_font_size=96` 且 `caption_min_font_size=96`。
- **规则③（介绍文案内部间距）**：介绍文案之间的行间距必须保持一致，定版 `caption_item_gap_max=40`。文案内部间距必须小于或等于“标题到第一条文案”的间隙，也必须小于“最后一条文案到直播时间”的间隙；不能因为均分剩余空间把文案之间拉得比上下外部间隙还大。图1-4和图5必须使用同一视觉规则。生成前必须检查 `generate_image.py` 中图1-4通用逻辑的 `_gap_between` 上限仍为40，不能回到旧值120；`template_config.py` 中模板1-4的 `bullet_spacing` 初始值为144，不能回到旧值160。
- **规则④（标题到介绍文案间隙）**：图1-4 的标题到介绍文案间隙不能贴得太近，必须和图5保持一致。定版：标题一行时 `title_caption_gap_min=90`；标题两行时 `title_caption_gap_min_two_lines=120`。若标题是一行，也不能把第一条介绍文案顶到标题下面；若标题是两行，必须主动拉开到至少120px，避免视觉上偏挤。
- `title_max_lines=1`（强制标题压一行、字号骤减）**只在用户当期明确要求时才用，绝不延续到其它期**；默认不传（=两行大字号）。

**7. 模板5内容垂直位置（2026-07-06）**：config 加 `t5_content_shift`（默认0），标题+文案整体下移该像素数；当前 t5=30。

**8. 横版(t6)二维码框（2026-07-06 定版）**：qr_box=`[2468, 403, 880, 880]`——二维码要**缩到框内、四周留白约30px**，不能填满外框（否则白色静默区盖住蓝色边框）。QR框外边框实测 x2436~3381、y368~1309。曾经放920填满→盖边框，缩到880居中+下移5px才合适。

**9. 关键词颜色**：DATE_CODE_COLOR=(252,159,81)，5张关键词与各模板括号色一致、勿改。横版在白底上视觉偏浅是错觉，用户已确认保持不动。

**10. 实际执行目录**：当前 Codex 实际运行工具目录为 `/Users/fanlili/.codex/skills/live-poster-codex`。备份目录只用于存档和分享，不作为生成海报的运行源；如果看到命令、metadata 或脚本默认值指向 `/Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool` 这类旧备份路径，必须改用当前技能目录。

详细坑与校准脚本见 `memory/feedback_live_poster_layout.md` 和 `memory/feedback_live_poster_workflow.md`。

## 🔴 2026-07-09 工作流复盘固化（459期起，必须遵守）

本节优先级高于下方历史流程文字。涉及飞书文档写入、群消息发送、定时任务时必须逐条执行。

### 0. 群消息必须使用直播物料小助手机器人（最高优先级）

从 2026-07-20 起，直播流程里**所有发到飞书群的消息和图片，必须使用机器人身份发送**，不得使用范丽丽用户身份。

- 机器人名称：直播物料小助手
- App ID：`cli_a94b5144b1381cb3`
- 本地 profile：`live-poster-bot`
- 发送命令统一使用：`/Users/fanlili/.npm-global/bin/lark-cli --profile live-poster-bot ... --as bot`

适用范围：
- 内容小分队 @汤爱学 / @郭凤强 复核：文字、post @、图片
- PPT制作群 @胡亮 / @郭凤强：配置消息、海报上传通知
- 226课程群 @李菁：第一条通知、第二条预告帖正文、第三条学院海报图片
- 螺丝钉团队大群：17:00 小助手安排文字、学院海报图片
- 汤爱学确认轮询、二维码消息读取等群消息读取动作，优先用 `--profile live-poster-bot --as bot`

不适用范围：
- 飞书文档读取/写入、表格读取、云盘上传等资源操作仍按原流程使用范丽丽用户身份，除非明确验证 bot 权限可用。
- 但只要动作是“发到群里”，无论发送内容来自哪里，都必须切回 `--profile live-poster-bot --as bot`。

禁止事项：
- 禁止用默认 profile 发群消息。
- 禁止用 `--as user` 发群消息。
- 禁止为了方便上传图片而改用范丽丽身份发送课程群、PPT群、内容小分队或团队群消息。
- 如果某条群消息误用用户身份发出，必须立即暂停后续群发，向用户说明“哪几条已用 user 发出 / 哪些未发 / 后续待办是什么”，并询问用户是否重发、补发、继续或跳过。不得自行决定整条流程作废，也不得继续假装流程正常。

发 post 消息必须继续使用 at 标签，不能因为换成 bot 就写纯文字 @：

```bash
/Users/fanlili/.npm-global/bin/lark-cli --profile live-poster-bot im +messages-send \
  --as bot \
  --chat-id oc_xxx \
  --msg-type post \
  --content '{"zh_cn":{"title":"","content":[[{"tag":"at","user_id":"ou_xxx","user_name":"姓名"},{"tag":"text","text":" 消息内容"}]]}}'
```

发送图片也使用 bot profile：

```bash
/Users/fanlili/.npm-global/bin/lark-cli --profile live-poster-bot im images create --as bot --data '{"image_type":"message"}' --file "image=output/xxx.png"
/Users/fanlili/.npm-global/bin/lark-cli --profile live-poster-bot im +messages-send --as bot --chat-id oc_xxx --image "{image_key}"
```

发送后必须核验：
- 每次发出关键群消息后，必须用 `im +messages-mget --as bot --message-ids <message_id>` 或读取群最新消息核验发送者。
- 验收标准：`sender.sender_type == "app"` 且 `sender.name == "直播物料小助手"`。
- 只看到命令返回 `identity: bot` 不够，必须看消息本身的 `sender`。
- 2026-07-20（462期）教训：曾只核验了 PPT 制作群，漏核 226 课程群，导致 226 课程群三条消息显示为范丽丽。之后所有群消息必须逐条按 sender 核验。

### 0.1 群消息状态检查默认 30 分钟轮询

群消息状态检查默认**不启用实时事件监听**。直播流程对汤爱学/郭凤强复核、胡亮二维码等消息的时效要求是半小时内抓到即可，因此使用稳定的轮询方式：

- 默认每 30 分钟检查一次相关群最新消息
- 统一使用 `直播物料小助手` bot 身份读取：`/Users/fanlili/.npm-global/bin/lark-cli --profile live-poster-bot im +chat-messages-list --as bot ...`
- 检查范围包括：
  - 内容小分队：汤爱学 / 郭凤强是否回复 `OK` / `ok` / `没问题`
  - PPT制作群：胡亮是否发送三张二维码图片
  - 后续流程中需要确认的其他群消息
- 命中后继续下一步；未命中则 30 分钟后再查
- 18:00 后仍未命中，停止自动轮询并告知用户手动确认
- 除非用户明确要求“实时监听/马上收到消息”，否则不要启动 `lark-cli event consume` 常驻监听进程

### 1. 物料写入前必须先窗口预览

直播物料生成后，**先在对话窗口完整发给用户预览**，包括标题、小助手配置、同步胡亮信息、学院预告帖正文、关键词、直播链接和所有空行格式。

只有用户明确回复「确认 / ok / 好的」后，才允许把物料插入飞书物料文档。

飞书物料文档插入位置规则：
- 插入位置必须是置顶内容之后、前一期标题之前
- 插入前必须保留零宽空格空行，避免飞书 block 结构把内容堆成一整段
- 插入后必须重新 fetch 文档顶部，确认本期在前一期前面，且空行没有塌

### 2. 写动作遇到歧义必须先确认

凡是会发飞书消息、上传文件、修改文档、创建定时任务的动作，如果用户表述有任何歧义，必须先问一句确认，不要自行推断。

特别注意：
- 「发在课程群里吧」在海报复核上下文中可能有歧义，必须确认是“正式发 226 课程群”，还是“发内容小分队复核”
- 复核阶段默认发内容小分队 @汤爱学 / 按用户指定 @郭凤强，不要误发课程群
- 一旦误发或发现方向错，先停下来，列清楚“已发 / 未发 / 下一步”，再继续

### 3. 内容小分队海报复核

海报生成后，如需发内容小分队复核：
- 群：`oc_d49775cef6a606b893cdec743875be02`
- 必须使用 post 格式 + at 标签 @复核人，纯文字 @ 不算
- 汤爱学 open_id：`ou_e9a6dd9bd4ab1b8d65452635ef70c953`
- 郭凤强 open_id：`ou_ac59bb01b7e830ae90f51515e0b54a07`
- 发送顺序：先发复核文字，再发 6 张海报图片
- 图片上传和发图都使用 `--profile live-poster-bot --as bot`

### 4. 226课程群必须严格三步

226课程群 chat_id：`oc_ad278b7a15d31ab7a5ced19569769db8`

正式发课程群时必须按以下顺序：
1. 第一条：只发李菁通知文字，post 格式 @李菁，不附图片，必须 `--profile live-poster-bot --as bot`
2. 第二条：只发学院直播预告帖正文，必须 `--profile live-poster-bot --as bot`
3. 第三条：紧接着发学院海报图片，上传图片和发送图片都必须 `--profile live-poster-bot --as bot`

李菁 open_id：`ou_93a19e195953359b82d943d7dff11b87`

三条发送完成后，必须逐条核验 sender：
- `sender.sender_type` 必须为 `app`
- `sender.name` 必须为 `直播物料小助手`
- 如果任何一条显示为范丽丽或其他 user，立即暂停后续群发，先向用户说明并等待处理；同时必须列出后续仍未完成事项，尤其是“螺丝钉团队群 17:00 定时任务/补发”和“最终备份”，不能因为暂停而漏掉。

如果用户删除了某条并要求重发，必须按用户要求补发对应条目；如果要保持完整顺序，应明确说明哪些旧消息仍在群里，避免重复混乱。

### 5. 团队群 17:00 定时任务必须真正 @ 对应小助手

直播当天 17:00 必须给螺丝钉团队群发送小助手安排 + 学院海报。

团队群 chat_id：`oc_fcace7d6268518d54ed1998b29764543`

定时任务消息必须：
- post 格式
- @所有人
- 1号李菁不需要 @
- 除 1号外，本期小助手必须用 at 标签真正 @ 到对应人员，不能只写名字
- 禁止出现“辅助”两个字。“辅助”只是内部排期说法，团队群通知里 3号/7号也只写“（回复用户提问）”，不要写“辅助”

小助手 open_id 对照表：
- 1号 李菁：`ou_93a19e195953359b82d943d7dff11b87`（团队群通知中不 @）
- 3号 汤爱学：`ou_e9a6dd9bd4ab1b8d65452635ef70c953`
- 5号 杜亚芳：`ou_edb1b102186805f9e1d0a41dd2eae6e8`
- 6号 食米马伞：`ou_d62113f91cb152e6424966002d9a96ed`
- 7号 范丽丽：`ou_2bd618ee347cc81fe4f1832ef1f35c91`
- 8号 马丹：`ou_9fe12ae756fbc395316efdcf0ec1353d`
- 9号 王恒一：`ou_98dc9bd42cce1710e7c12f6cf55c3694`

团队群通知职责文案定版：
- 1号：红色（发PPT图片，控场，回复用户提问）
- 3号/5号/6号/7号/8号/9号：对应颜色（回复用户提问）
- 任何编号后面都不要追加“辅助”等内部角色说明

如果当前会话没有 `mcp__scheduled-tasks__create_scheduled_task`，可以创建本地 scheduled-task `SKILL.md` 作为记录；若需要真正自动执行，可用 macOS LaunchAgent 注册一次性任务，但必须验证 launchd 已加载成功。

macOS LaunchAgent 注意事项（2026-07-10 补充）：
- LaunchAgent 的运行环境不会继承终端 PATH。脚本里必须显式加入：
  `export PATH="/usr/local/bin:/opt/homebrew/bin:/Users/fanlili/.npm-global/bin:/usr/bin:/bin:/usr/sbin:/sbin"`
- 否则 `lark-cli` 可能因 shebang `env node` 找不到 `/usr/local/bin/node` 而失败，表现为定时任务触发了但没有发飞书消息，日志里会出现 `env: node: No such file or directory`。
- **北京时间硬校验（2026-07-28 新增，最高优先级）**：团队群 17:00 通知的业务时间永远是北京时间。LaunchAgent / Cron / 本机时区都只能作为“唤醒器”，不能作为最终发送依据。发送脚本在任何飞书发送动作之前，必须用 `Asia/Shanghai` 目标时间计算绝对 epoch：
  - 若当前时间早于目标北京时间：脚本必须 sleep 到目标时间后再发送，不能直接退出，也不能提前发送。
  - 若当前时间晚于目标北京时间超过 30 分钟：脚本必须停止发送并记录日志，不能补发；需通知用户手动判断。
  - 若当前时间在目标后 30 分钟内：允许发送，并发送后核验群消息 sender。
  - 禁止只把 `2026-07-28T17:00+08:00` 手工换算成本机 `Hour/Day` 后就认为完成；必须在脚本内做北京时间 epoch 守门。
  - 脚本模板必须包含如下守门逻辑（把 `TARGET_BJ` 替换为当期直播日期）：
    ```bash
    TARGET_BJ="2026-07-28 17:00:00"
    TARGET_EPOCH="$(/usr/bin/python3 - <<'PY'
from datetime import datetime
from zoneinfo import ZoneInfo
target = datetime.strptime("2026-07-28 17:00:00", "%Y-%m-%d %H:%M:%S").replace(tzinfo=ZoneInfo("Asia/Shanghai"))
print(int(target.timestamp()))
PY
)"
    NOW_EPOCH="$(/bin/date +%s)"
    if [ "$NOW_EPOCH" -lt "$TARGET_EPOCH" ]; then
      SLEEP_SECONDS=$((TARGET_EPOCH - NOW_EPOCH))
      echo "early trigger; sleep ${SLEEP_SECONDS}s until Beijing target ${TARGET_BJ}"
      /bin/sleep "$SLEEP_SECONDS"
    fi
    NOW_EPOCH="$(/bin/date +%s)"
    LATE_SECONDS=$((NOW_EPOCH - TARGET_EPOCH))
    if [ "$LATE_SECONDS" -gt 1800 ]; then
      echo "too late (${LATE_SECONDS}s after Beijing target ${TARGET_BJ}); abort sending"
      exit 0
    fi
    ```
- 创建后必须检查：`launchctl print gui/$(id -u)/{label}`，确认任务已加载；执行后必须查看日志，确认飞书文字和图片都已发出。
- LaunchAgent 的 `StartCalendarInterval` 不含年份，一次性任务执行完成后必须 `bootout` 并删除 plist，避免次年同月同日再次触发。

2026-07-10 漏发复盘（必须吸取）：
- 459期团队群 17:00 定时通知实际已触发，日志显示 `17:00:04 start live-poster-459-team-notify`，但因 LaunchAgent 环境找不到 `node`，`lark-cli` 启动失败，最终没有发出飞书消息。
- 根因不是“没有创建定时任务”，而是只验证了任务创建/加载，没有验证脚本在定时器环境中能完整跑通，也没有在到点后确认飞书消息真实发出。
- 下次创建任何自动发送类定时任务后，必须完成四步验收：
  1. 创建后检查任务已加载；
  2. 检查脚本内显式设置 PATH，且包含 `node` 与 `lark-cli` 所在目录；
  3. 到点后检查日志没有报错；
  4. 到点后检查目标飞书群里确实出现了文字消息和图片消息。
- 定时任务的验收标准是“目标群已收到消息”，不是“任务已创建”。如果无法自动确认目标群消息，必须及时告知用户手动确认，不得默认成功。

2026-07-28 464期团队群提前发送复盘（必须修正）：
- 用户要求北京时间 2026-07-28 17:00 发送团队群通知，实际在北京时间 2026-07-28 02:09 左右发出，提前约 15 小时。
- 直接原因：只依赖 LaunchAgent 的 `StartCalendarInterval`，并手工设置本机 `Month=7 Day=28 Hour=2 Minute=0`；到点行为与预期时区不一致。
- 根本问题：脚本没有在发送前使用北京时间目标 epoch 做硬校验，导致唤醒器异常时没有防线。
- 后续所有团队群定时发送都必须使用“唤醒器 + 北京时间 epoch 守门”双层结构。只有 epoch 守门通过后才允许调用飞书发送接口。
- 定时任务日志中必须同时记录本机时间、北京时间、目标北京时间、早到 sleep 秒数或超时退出原因。
- 发送前如无法确认当前北京时间已经达到目标时间，宁可不发并提示用户，也不能提前发。

### 6. 已执行记录

464期关键信息：
- 直播日期：2026年7月28日（周二）
- 标题：主动基金风格漂移，该怎么办？
- 关键词：260728
- 小助手：5、9、7
- 回放文章：否
- 翻写文章：是，直播后3天
- 飞书共享文件夹：`https://epndqwwg0a.feishu.cn/drive/folder/KgH6fLkV1lZ5o9dfhircGEKznfg`
- PPT制作群/226课程群三条消息：2026-07-26 已用直播物料小助手 bot 发送并核验 sender（均为 app/直播物料小助手）
- 团队群通知：`live-poster-464-team-notify`（launchd，`/Users/fanlili/live-poster-scheduled/`），计划 2026-07-28 17:00 北京时间
- 备注：本次由 2026-07-27 例行巡检发现步骤1-4均已于前一晚完成，仅补齐第5/6步（SKILL记录+备份）

463期关键信息：
- 直播日期：2026年7月24日（周五）
- 标题：成长、价值风格轮动，我们该如何投资？
- 关键词：260724
- 小助手：5、8、7
- 回放文章：否
- 翻写文章：是，直播后一周
- 飞书共享文件夹：`https://epndqwwg0a.feishu.cn/drive/folder/G740feQ2ulLK0DddcPVc8qwgnUg`
- 团队群通知：`live-poster-463-team-notify`（launchd），已于 2026-07-24 触发
- 备注：本期课程群/PPT群消息使用了范丽丽用户身份发送（早于0号bot规则严格执行期），2026-07-27 例行巡检补齐第5/6步（SKILL记录+备份）

462期关键信息：
- 直播日期：2026年7月21日（周二）
- 标题：这轮牛市结束了吗？
- 关键词：260721
- 小助手：6、9、3
- 回放文章：是，直播当日
- 翻写文章：是，直播后一周
- 飞书共享文件夹：`https://epndqwwg0a.feishu.cn/drive/folder/XflFfeOeQlqQJfdMHobcWB4qncc`
- 内容小分队海报复核：汤爱学 2026-07-20 17:40 回复“没有问题”
- PPT制作群通知：2026-07-20 已用直播物料小助手 bot 发送并核验 sender
- 226课程群通知：2026-07-20 三条误用范丽丽用户身份发送，已复盘写入 10.1；后续不得复发
- 团队群通知：原 17:00 定时任务漏建，2026-07-21 17:25 已用直播物料小助手 bot 补发文字和学院海报，并核验 sender；已复盘写入 10.2

461期关键信息：
- 直播日期：2026年7月17日（周五）
- 标题：中证全指，投资价值如何？
- 关键词：260717
- 小助手：6、8、7
- 回放文章：是，直播当天
- 翻写文章：是，直播后一周
- 飞书共享文件夹：`https://epndqwwg0a.feishu.cn/drive/folder/LcFKfDwMFl1Gz7dAjJAcPMPunPc`
- 团队群定时任务：`live-poster-461-team-notify`，计划 `2026-07-17T17:00:00+08:00`

460期关键信息：
- 直播日期：2026年7月14日（周二）
- 标题：面对市场波动，我们该怎么办？
- 关键词：260714
- 小助手：5、9、3
- 回放文章：是，直播当日
- 翻写文章：是，直播后3天
- 飞书共享文件夹：`https://epndqwwg0a.feishu.cn/drive/folder/Z3dEfFKcylBqnsdaE6VcASdwnZd`
- 团队群定时任务：`live-poster-460-team-notify`，计划 `2026-07-14T17:00:00+08:00`

459期关键信息：
- 直播日期：2026年7月10日（周五）
- 标题：大家都定投，A股的波动会变小吗？
- 关键词：260710
- 小助手：5、8、7
- 回放文章：否
- 翻写文章：是，直播后3天
- 飞书共享文件夹：`https://epndqwwg0a.feishu.cn/drive/folder/MiOZfhz3YldRePdmjjQcfOFkn2b`
- 团队群定时任务：`live-poster-459-team-notify`，计划 `2026-07-10T17:00:00+08:00`

### 7. 字体压缩默认禁止

后续没有用户特殊说明时，**不要压缩字体**。

具体规则：
- 标题默认保持设计字号：竖版 148、横版 180
- 介绍文案默认保持 96，且最小为 96
- 关键词默认保持 116
- 不要为了少占一行、避免换行、避免孤字而把介绍文案缩到 96 以下
- 如果出现文字放不下、孤字、行宽不够等问题，优先调整 `title_max_width` / `caption_max_width`、手动换行，或停下来让用户确认
- 只有用户当期明确说“压缩字号 / 标题压一行 / 字可以小一点”时，才允许触发缩字号方案；即使触发，介绍文案默认仍不得低于 96，除非用户明确同意低于 96，且不得延续到后续期数

### 8. 复核确认后必须自动进入下一步（2026-07-16 补充）

内容小分队或用户给出明确确认后，必须自动继续后续流程，不要停在“已确认，请用户提醒我继续”。

明确确认包括：
- 汤爱学/郭凤强在对应复核消息下回复 `OK` / `ok` / `没问题` / `没有问题` / `确认` / `无误` / 语义等价表述
- 用户在当前对话回复 `OK` / `确认` / `好的`

确认后自动执行：
- 物料复核确认后：立即发 PPT 制作群 @胡亮，并启动二维码检测
- 海报确认后：立即上传飞书共享空间并通知胡亮/郭凤强
- 课程群发布确认后：继续发完整课程群消息、创建团队群17:00定时任务、最终备份

不要把“检测到确认”当作流程终点；它只是进入下一步的触发条件。

### 9. 海报生成前必须使用最新工具包并做布局自检（2026-07-16 补充）

生成海报前必须确认实际运行的工具包路径和规则是最新的，不得误用过期备份导致旧问题复发。

本次 461 期复盘：
- 曾误用备份目录中的旧配置，图1-4介绍文案间距又被拉大。原因：旧代码 `_gap_between` 上限仍可能到120，旧配置 `bullet_spacing=160`。
- 曾横版标题框仍为旧宽度1850，导致“？”/单字掉到第二行。修正：模板6 `title_max_width=2200`，左侧固定，只向右扩，字号保持180。

每次生成前必须自检：
- 实际运行目录是 `/Users/fanlili/.codex/skills/live-poster-codex`，不是自动化备份目录里的旧副本
- `template_config.py`：模板1-4 `bullet_spacing=144`
- `generate_image.py`：图1-4 `_gap_between` 最大值为40，并且不大于标题到文案间隙
- `template_config.py`：模板6 `title_max_width=2200`
- 生成后至少检查横版标题行数，不能出现单字/标点独占一行

### 10. 2026-07-21 最近问题复盘与防错清单（462期后补充）

本节用于约束执行方式，不是事后解释。后续每期必须按清单执行，遇到异常时多问用户，不要自行扩展规则或自作主张。

#### 10.1 462期课程群身份错误

现象：
- 226课程群三条消息（李菁通知、学院直播预告帖正文、学院海报）误用范丽丽用户身份发送，群内显示“范丽丽”。
- PPT制作群通知是 bot 身份，但只核验 PPT 群导致误判“所有群消息都没问题”。

根因：
- 上传飞书共享空间使用范丽丽用户身份是允许的，但上传后发群消息时没有显式切回 `--profile live-poster-bot --as bot`。
- 发后核验只抽查了 PPT 制作群，没有逐条核验 226课程群三条消息的 `sender`。

硬规则：
- 发群消息前必须先写出/确认命令前缀：`/Users/fanlili/.npm-global/bin/lark-cli --profile live-poster-bot ... --as bot`。
- 发群消息后必须逐条核验消息本身 `sender.sender_type == "app"` 且 `sender.name == "直播物料小助手"`。
- 上传云盘、改文档、读表格可以使用范丽丽用户身份；但只要动作是“发到群里”，必须切回 bot 身份。
- 如果发现身份错发，不要自行决定后续怎么处理。必须暂停、列出已发/未发/建议处理方式，并询问用户是否重发、补发、继续或跳过。

#### 10.2 462期团队群17:00通知漏建

现象：
- 2026-07-21 17:00 螺丝钉团队群小助手安排没有按时发出。
- 排查发现 `~/Library/LaunchAgents` 和 `.claude/scheduled-tasks` 均没有 462 期团队通知任务。

根因：
- 发现课程群身份错误后，流程被暂停，但没有维护“未完成事项清单”，导致第十二步团队群 17:00 定时任务没有创建。
- 结束前没有检查 `live-poster-462-team-notify` 是否存在，也没有检查团队群是否已收到通知。

硬规则：
- 从海报确认后开始，必须维护本期期数执行清单：
  1. 上传飞书共享空间完成；
  2. PPT制作群通知完成并核验 sender；
  3. 226课程群三条完成并逐条核验 sender；
  4. 团队群 17:00 定时任务已创建并验证，或若已过 17:00，先询问用户是否立即补发；
  5. SKILL 当前最新期数和已确认记录已更新；
  6. 完整压缩包备份已生成。
- 不得把“发现错误暂停”理解成“剩余步骤作废”。暂停时必须把未完成事项列给用户。
- 如果发现已过定时点且消息未发，除非用户明确要求立即补发，否则先问用户是否补发；补发时仍必须用 bot 并核验 sender。

#### 10.3 不要自作主张扩展规则

用户已明确要求：以后不要自作主张，多问用户。

执行要求：
- 遇到发送对象、发送身份、是否重发/撤回、是否补发、是否跳过、是否继续后续步骤有不确定性时，先问用户。
- 询问时必须给出简短状态：已完成什么、出错什么、还缺什么、建议选项是什么。
- 不得把临时纠错策略写成会跳过后续必要步骤的硬规则。
- 规则更新必须服务于防错；如果规则会改变用户可见流程、发送对象、发送身份、是否继续，必须先征得用户确认。

#### 10.4 备份口径

用户此前已明确：之后说“备份”，默认指完整压缩包备份，不是只备份单独 md。

执行要求：
- 备份产物必须放在 `/Users/fanlili/Desktop/范丽丽./自动化备份/`。
- 备份包必须包含完整可运行 skill/工具内容：`SKILL.md`、memory、Python 脚本、模板图、字体、requirements、README、必要配置。
- 同事可用包必须能直接发给同事，让对方交给自己的 AI 使用；不能只放一份 md。
- 打包前必须从当前实际运行目录 `/Users/fanlili/.codex/skills/live-poster-codex` 取最新文件，不得从旧备份目录反向覆盖。

---

## 自动化工作流

### 定时检测

通过 Cron 定时任务自动检测群内新直播消息：

- **执行时间**：周一、周四 13:00-18:00，每30分钟检查一次
- **具体时间**：13:00, 13:30, 14:00, 14:30, 15:00, 15:30, 16:00, 16:30, 17:00, 17:30
- **其他时间**：不执行检查

### 手动触发

当用户在群里发布新直播通告时，自动执行以下流程：

## 完整流程

### 1. 检测新直播消息

从飞书群"内容|PPT制作群"（群ID: oc_918c9be8ab6950e746bc308c8c32a334）读取最新直播通告消息。

与已记录的最近期数对比，如果更新则继续。

### 2. 读取群消息获取直播信息

消息特征：
- 包含"直播标题"字段
- 包含"直播间链接"
- 包含"推广文案"
- 包含"关键词"

### 3. 使用 smart_parser 解析直播信息

```python
import sys
sys.path.insert(0, '/Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool')
from smart_parser import smart_parse_notice

# 解析
data = smart_parse_notice(text)
# 返回: title, captions, live_time, link, date_code, issue_number
```

### 4. 下载二维码

从群里下载两个二维码：
- 直播预告二维码（群里最新的预告二维码）
- 翻写文二维码（群里最新的翻写文二维码）

保存为：
- qr_1_yugao_{期数}.png（直播预告）
- qr_2_fanxie_{期数}.png（翻写文）

如需从链接生成二维码：
```python
import qrcode
link = "https://xxx"
qr = qrcode.QRCode(box_size=10, border=1)
qr.add_data(link)
qr.make(fit=True)
img = qr.make_image(fill_color="black", back_color="white")
img.save("qr_link.png")
```

### 5. 生成海报（历史示例，正式流程按顶部最新定版生成6张）

```python
import sys
sys.path.insert(0, '/Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool')
from generate_image import create_poster
from template_config import get_template_config

# 解析后的数据
title = data['title']           # 直播标题
live_time = data['live_time']   # 直播时间
date_code = data['date_code']   # 关键词/日期码
captions = data['captions']     # 推广文案（4句）
issue = data['issue_number']    # 期数

# 二维码映射（长期规则，勿修改）
qr_map = {
    "template_final": "qr_link_{}.png".format(issue),      # 学院：用直播链接生成的二维码
    "template_2": "qr_2_fanxie_{}.png".format(issue),     # 回放：用群里第二个二维码
    "template_3": "qr_1_yugao_{}.png".format(issue),      # 预告+企微朋友圈：用群里第一个二维码
    "template_4": "qr_1_yugao_{}.png".format(issue),      # 翻写：用群里第一个二维码
    "template_5": "qr_1_yugao_{}.png".format(issue),      # 新预告：用群里第一个二维码
}

# 生成所有海报
templates = ["template_final", "template_2", "template_3", "template_4", "template_5"]

for tmpl_id in templates:
    config = get_template_config(tmpl_id)
    qr_file = qr_map[tmpl_id]
    output_file = "{}{}.png".format(issue, config['suffix'])
    
    create_poster(
        template_path=config["path"],
        qr_image_path=qr_file,
        output_path=output_file,
        title=title,
        caption_list=captions,
        live_time=live_time,
        template_id=tmpl_id,
        date_code=date_code
    )
```

### 6. 发送海报给你确认

1. 先发送一条文字消息确认信息：
```
📺 {期数}期直播海报已生成！
📅 直播时间：{时间}
🔗 链接：{链接}
🔑 关键词：{关键词}

请回复「1」或「确认」或「ok」继续后续操作～
```

2. 然后依次发送6张海报图片：
   - 学院（template_final）
   - 预告+企微朋友圈（template_2）
   - 回放（template_3）
   - 翻写（template_4）
   - 螺丝钉上（template_5）

3. **等待你确认** - 收到你的回复（1/确认/ok）后，继续执行下一步

### 7. 你确认后

1. **上传到飞书共享文档**：
   - 飞书共享文档目录：`https://epndqwwg0a.feishu.cn/drive/folder/MplmffLghlQ17Rd7blbcL6Umnhe`
   - 在该目录下找到对应期数的文件夹（如 `441-20260324`）
   - 上传6张海报到该文件夹

2. **发送通知**：
   - 发给你确认
   - 发到群里艾特@胡亮，格式：
   ```
   {月}.{日}直播海报已上传至共享文档了哈，请知悉～
   附：文件夹链接
   @胡亮
   ```

## 代码规则（已封装）

生成代码已按以下规则优化：

1. **标题**：字号自适应，字数多则自动调小，最多2行
2. **介绍文案**：字号根据字数多少自适应，字数多字号小，最多2行
3. **点点位置**：和文案整体居中对齐，再往下挪20px
4. **模板配置**：template_config.py 中已配置好5个模板的位置参数
5. **标题去重**：自动去除标题中连续重复字符（如"该该"->"该"）

## 模板五（预告）视觉规范（2026-04-20固化，长期复用）

**这是模板五的标准样式，以444期为基准确认。后续生成必须严格遵循此规范。**

### 布局结构

从上到下依次为：
1. **左上角**：主讲人头像（圆形裁剪） + 右侧个人介绍（姓名、奖项、书籍）
2. **中部居中**：直播主题标题（深蓝色粗体，居中对齐）
3. **标题下方**：3个核心问题（橙色圆点前缀，竖排）
4. **问题下方**：时间按钮（蓝色圆角矩形，白底白字，带图标）
5. **底部**：二维码（左侧） + 参与指引（右侧，含"长按识别"和橙色"回复「0414」"）

### 文字填充规则

| 元素 | 位置参数 | 颜色 | 字号 | 说明 |
|------|----------|------|------|------|
| 标题 | 居中，中心x=1459，y=440，最大宽度1125px | 深蓝色（#1F3864） | 自适应（字数多则小） | 最多2行，居中对齐 |
| 3句文案 | 固定y=[520, 580, 640]，左侧橙色圆点（x=116, r=6），文字x=145，最大宽度700px | 深蓝色 | 常规 | 竖排，带橙色圆点 |
| 时间按钮 | x=80, y=2340，框宽701x146 | 按钮内白底蓝字 | 加粗 | 格式："4月14日（周二）19:00" |
| 关键词 | x=80, y=2486，框147x114 | 橙色（#FC8414） | 加粗 | 纯日期码，如"0414" |
| 二维码 | x=294, y=2620，尺寸398x395 | — | — | 群里第一个预告二维码 |
| 二维码旁文字 | 二维码右侧 | 白字+橙色关键词 | 常规 | "长按识别二维码 回复「0414」" |

### 配色参考

- 主色（背景）：深蓝色 `#1F3864`
- 内容区背景：白色
- 强调色：橙色 `#FC8414`
- 正文：深蓝色
- 装饰：浅蓝色圆点 + 半透明"LUO SI DING"文字水印

### 二维码规则（重要，必须遵守）

| 模板 | 用途 | 二维码来源 |
|------|------|-----------|
| template_final (模板1) | 学院 | 链接生成的二维码 (qr_link_{期数}.png) |
| template_2 (模板2) | 预告+企微朋友圈 | 第二个二维码 (qr_2_fanxie_{期数}.png) |
| template_3 (模板3) | 回放 | 第一个二维码 (qr_1_yugao_{期数}.png) |
| template_4 (模板4) | 翻写 | 第一个二维码 (qr_1_yugao_{期数}.png) |
| template_5 (模板5) | 预告 | 第一个二维码 (qr_1_yugao_{期数}.png) |

**关键规则**：模板2/3/5 用第一个二维码（预告二维码），模板4用第二个二维码（翻写二维码）。

## 模板文件位置

- 模板1: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_1_latest.png
- 模板2: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_2_latest.png
- 模板3: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_3_latest.png
- 模板4: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_4_latest.png
- 模板5: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_5_latest.png（预告风格，螺丝钉上）

## 字体配置

- 优先使用思源黑体（Source Han Sans SC）：
  - 粗体：`/tmp/fonts/shs/OTF/SimplifiedChinese/SourceHanSansSC-Bold.otf`
  - 常规：`/tmp/fonts/shs/OTF/SimplifiedChinese/SourceHanSansSC-Regular.otf`
- 兜底字体：Noto Sans CJK（`/usr/share/fonts/opentype/noto/NotoSansCJK-*.ttc`）
- generate_image.py 中已配置自动回退逻辑

## 模板五布局参数（template_config.py 中已配置，直接复用）

**这是模板五的标准样式，以444期为基准确认。后续生成必须严格遵循此规范。**

### 布局结构

从上到下依次为：
1. **左上角**：主讲人头像（椭圆形） + 右侧个人介绍（姓名、头衔、作品）
2. **中部居中**：直播主题标题（深蓝色粗体，居中对齐）
3. **标题下方**：3个核心问题（橙色圆点前缀，竖排）
4. **问题下方**：时间按钮（蓝色圆角矩形，白底蓝字，带图标）
5. **底部**：二维码（左侧） + 参与指引（右侧，含"长按识别"和橙色"回复「0414」"）

### 文字填充规则

| 元素 | 位置参数 | 颜色 | 字号 | 说明 |
|------|----------|------|------|------|
| 标题 | 居中，中心x=1200，y=420，最大宽度2200px | 深蓝色（#1A3B8E） | 自适应（字数多则小） | 最多2行，居中对齐 |
| 3句文案 | 固定y=[600, 820, 1040]，左侧橙色圆点（x=200，r=10），文字x=240，最大宽度1900px | 深蓝色 | 常规 | 竖排，带橙色圆点 |
| 时间按钮 | x=80, y=1900，宽2219×449 | 按钮内白底蓝字 | 108pt | 格式："4月14日（周二）19:00" |
| 关键词 | x=80, y=2550，框147×114 | 橙色（#FC9F51） | 115pt | 纯日期码，如"0414" |
| 二维码 | x=80, y=2400，尺寸569×641 | — | — | 群里第一个预告二维码 |
| 二维码旁文字 | 二维码右侧 | 白字+橙色关键词 | 常规 | "长按识别二维码 回复「0414」" |

### 配色参考

- 主色（背景）：深蓝色 `#1A3B8E`
- 内容区背景：白色
- 强调色：橙色 `#FC9F51`
- 正文：深蓝色
- 装饰：半透明装饰元素

### 二维码规则（重要）

| 模板 | 用途 | 二维码来源 |
|------|------|-----------|
| template_final (模板1) | 学院 | 链接生成的二维码 (qr_link_{期数}.png) |
| template_2 (模板2) | 预告+企微朋友圈 | 第二个二维码 (qr_2_fanxie_{期数}.png) |
| template_3 (模板3) | 回放 | 第一个二维码 (qr_1_yugao_{期数}.png) |
| template_4 (模板4) | 翻写 | 第一个二维码 (qr_1_yugao_{期数}.png) |
| template_5 (模板5) | 预告 | 第一个二维码 (qr_1_yugao_{期数}.png) |

**关键规则**：模板2/3/5 用第一个二维码（预告二维码），模板4用第二个二维码（翻写二维码）。

## 模板文件位置

- 模板1: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_1_latest.png
- 模板2: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_2_latest.png
- 模板3: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_3_latest.png
- 模板4: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_4_latest.png
- 模板5: /Users/fanlili/Desktop/范丽丽./自动化备份/live-poster-tool/template_5_latest.png（螺丝钉上预告风格，2026-04-22更新）

## 关键词位置

- template_2: date_code_box = [945, 4153, 344, 106]，无白色背景
- template_3: date_code_box = [823, 4153, 344, 106]
- template_4: date_code_box = [833, 4153, 344, 106]
- template_5: date_code_box = [80, 2486, 147, 114]（位于时间按钮下方，橙色）

## content_y_offset（文案垂直位置偏移）

- template_final: -30
- template_2: -40
- template_3: -40
- template_4: -40
- template_5: 0（固定y位置）

## 已确认期数记录

- 464期（7月28日）- 已确认
- 463期（7月24日）- 已确认
- 462期（7月21日）- 已确认（团队群17:00通知漏建，17:25已补发并复盘）
- 461期（7月17日）- 已确认
- 460期（7月14日）- 已确认
- 459期（7月10日）- 已确认
- 458期（7月7日）- 已确认
- 457期（手动完成）- 已确认
- 456期（6月16日）- 已确认
- 455期（6月12日）- 已确认
- 454期（6月9日）- 已确认
- 453期（6月5日）- 已确认
- 452期（6月2日）- 已确认
- 451期（5月26日）- 已确认
- 450期（5月22日）- 已确认
- 445期（4月17日）- 已确认
- 444期（4月14日）- 已确认（含模板五预告）
- 443期（4月10日）- 已确认
- 441期（3月24日）- 已确认
- 440期（3月20日）- 已测试确认流程

**当前最新期数：464**
- 检测到比 464 更高的期数才触发生成（如 465...）
- 低于或等于 464 的期数不重复生成

**模板五更新记录**：
- 2026-04-22：用新参考图替换模板五，更新了参数配置

（每次确认后更新此记录）

## 模板更新流程

如果用户给了新模板：
1. 保存为 template_1_latest.png ~ template_5_latest.png
2. 更新 template_config.py 中的路径配置
3. 更新 SKILL.md 中的模板文件位置
4. 记录到 memory/YYYY-MM-DD.md
